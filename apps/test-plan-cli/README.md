# kdtp-plan CLI

Internal JSON CLI for durable planning orchestration. Runtime composition is supplied through `KDTP_PLAN_CLI_CONTEXT_MODULE`; command handlers do not duplicate planner, governance or persistence logic.
