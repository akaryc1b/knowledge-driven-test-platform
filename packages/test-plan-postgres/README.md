# @kdtp/test-plan-postgres

PostgreSQL 18 durable adapter for the Test Plan Registry. It provides checksum-protected migrations, revision CAS, row locking, append-only history and review evidence, restart recovery, and database enforcement of immutable plan bindings and frozen content.
