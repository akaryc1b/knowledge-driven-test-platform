# @kdtp/project-membership

M1-F 的项目目录、成员生命周期和默认拒绝授权领域包。

能力：

- 项目与成员 Port；
- 项目和成员 revision CAS；
- 固定基础角色到治理动作映射；
- 成员状态与有效期窗口；
- deny-by-default 授权；
- 内存适配器与可复用合同测试。

该包不包含身份认证、HTTP API、组织同步或管理后台。
