# @kdtp/test-plan-registry

Transport-independent Test Plan Registry lifecycle, ports, in-memory adapter and shared adapter contracts. It stores validated deterministic planning results and governs durable record revisions; it does not execute tests or authorize reviewers.
