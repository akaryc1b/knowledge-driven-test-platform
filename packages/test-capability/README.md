# @kdtp/test-capability

Versioned, deterministic and executor-independent capability catalog contracts for M2 planning.

The package defines exact capability identity, catalog digests, target-kind compatibility and an in-memory `CapabilityCatalogPort` adapter. It intentionally contains no executor scripts, credentials, workers, queues, schedulers or runtime node configuration.
