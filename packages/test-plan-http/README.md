# @kdtp/test-plan-http

Read-only HTTP transport for Test Plan list, detail, coverage, provenance and timeline queries. It reuses the M1 authentication, rate-limit, request-safety and response-hardening primitives and exposes no plan write routes.
