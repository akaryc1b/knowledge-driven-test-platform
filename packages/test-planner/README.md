# @kdtp/test-planner

Pure deterministic planning from immutable published knowledge, target inventory, an exact capability catalog and planning policy into executor-independent intents, coverage, provenance and a dependency DAG.

This package performs no test execution, network access, database access, scheduling or AI generation.
